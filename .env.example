# Example environment variables
# PORT (the host will usually provide this)
PORT=8000

# AI analyst report — get a key at https://console.anthropic.com
# Without it, the AI report section simply does not render.
ANTHROPIC_API_KEY=
